

let initialState = {
    articles: [],
    loading: false,

};

const editPageBlog = (state = initialState, action) => {
    switch(action.type){

        default: return state;
    }
}

export default editPageBlog;